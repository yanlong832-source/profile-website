import {mkdir,writeFile} from "node:fs/promises";import {resolve} from "node:path";import fallback from "../src/data/issues.json" with {type:"json"};
const repo=process.env.GITHUB_ISSUES_REPO||"yanlong832-source/profile-website";const endpoint=`https://api.github.com/repos/${repo}/issues?state=all&labels=blog&per_page=100&sort=updated&direction=desc`;
try{const response=await fetch(endpoint,{headers:{Accept:"application/vnd.github+json","User-Agent":"profile-website-build"}});if(!response.ok)throw new Error(`GitHub API ${response.status}`);const issues=(await response.json()).filter(item=>!item.pull_request).map(item=>({number:item.number,title:item.title,body:item.body||"",labels:item.labels.map(label=>label.name),created_at:item.created_at,updated_at:item.updated_at,html_url:item.html_url}));await mkdir(resolve("src/data"),{recursive:true});await writeFile(resolve("src/data/issues.json"),JSON.stringify(issues.length?issues:fallback,null,2));console.log(`issues: ${issues.length||fallback.length} records`)}catch(error){console.warn(`issues: using fallback (${error.message})`)}
